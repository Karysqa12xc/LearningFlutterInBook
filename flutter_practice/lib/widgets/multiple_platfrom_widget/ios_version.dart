import 'package:flutter/cupertino.dart';

class IOSVersion extends StatelessWidget {
const IOSVersion({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}